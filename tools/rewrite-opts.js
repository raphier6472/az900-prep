// id -> new option texts, same order/position as the current bank (correct index unchanged). '*' marks the correct one for verification only.
module.exports = {
 128: ['A management group scope that groups the subscriptions', 'A resource group that groups the affected resources', 'A policy exemption that groups the excluded scopes', '*An initiative (policy set definition)'],
 55: ['*Azure Pricing Calculator', 'Azure Advisor cost recommendations', 'Azure Cost Management cost analysis', 'Azure Monitor usage and estimated costs'],
 58: ['It is the final, generally available version', 'It is a version that has been announced for retirement', '*It is an early version for testing, generally without a guaranteed SLA', 'It is a production-ready version with a full SLA'],
 78: ['Azure ExpressRoute with path-based routing rules', 'Azure Traffic Manager with a path-based routing profile', 'Azure Load Balancer with URL-aware health probes', '*Azure Application Gateway with URL path-based routing'],
 138: ['Azure Service Health in the portal, filtered to all regions', '*The Azure Status page (status.azure.com), no sign-in needed', 'Azure Monitor metrics explorer for the platform namespace', 'Azure Advisor reliability recommendations for the tenant'],
 39: ['*To group resources with the same lifecycle so they are managed and deleted together', 'To isolate resources into a separate billing account with its own invoice', 'To act as a network boundary that resources in other groups cannot cross', 'To define which region every resource inside it must be deployed to'],
 89: ['WAF sets the organizational governance model; CAF defines the technical pillars for each workload', 'CAF and WAF are the same guidance, published separately for enterprises and for small businesses', 'CAF applies only to migrations from on-premises; WAF applies only to applications built new in Azure', '*CAF guides the organizational strategy and process of adoption; WAF focuses on technical design principles for workloads'],
 102: ['Azure will rename the subnet to GatewaySubnet automatically during deployment', 'The gateway will deploy normally; the subnet name has no special meaning', '*The VPN Gateway deployment will fail — the subnet must be named exactly GatewaySubnet', 'The gateway will deploy, but only Point-to-Site connections will be supported'],
 54: ['A built-in RBAC role that removes delete permissions from every assignment', 'An encryption setting that makes a disk unreadable outside its resource group', '*A setting that prevents a resource from being accidentally deleted or modified', 'A policy effect that denies any change to a resource after it is created'],
 9: ['Elasticity', '*High availability', 'Fault isolation', 'Scalability'],
 13: ['Rapid elasticity of resources', '*Physical hardware dedicated permanently to one customer', 'Broad network access', 'On-demand self-service'],
 12: ['A pricing model where the discount grows with the number of subscriptions', 'A licensing program that bundles Windows Server with virtual machines', '*Lower per-unit costs thanks to Microsoft buying and operating at huge volume', 'A discount tier reserved for governments and large enterprises'],
 57: ['Virtual machines that are pre-allocated to guarantee capacity in a region', '*A discount in exchange for committing to use a resource for 1 or 3 years', 'An RBAC role that allows reserving capacity on behalf of a subscription', 'A type of dedicated host where instances are reserved for a single tenant'],
 49: ['Multi-factor authentication', '*Defense in depth', 'Role-based access control', 'Zero Trust'],
 137: ['*An alert rule (the condition) and an action group (who to notify and what to run)', 'A Cost Management budget and a budget alert with an automation webhook', 'Azure Advisor performance recommendations and a Service Health alert', 'A Log Analytics workspace query and a Resource Health notification'],
 56: ['*Configurable thresholds that trigger alerts as spending approaches or exceeds an amount', 'Monthly spending reports generated automatically and emailed to the subscription owner', 'Hard limits that automatically stop or deallocate resources when the amount is reached', 'Volume discount tiers that apply once a subscription crosses a monthly spending amount'],
 83: ['An Azure Policy with the deny effect assigned to the resource group', 'Conditional Access requiring MFA before any delete operation', 'The Contributor role does not include delete permissions on compute', '*A CanNotDelete resource lock applied to that resource or resource group'],
 97: ['*No — VNet peering is non-transitive; A and C would need their own direct peering', 'Yes — peering is transitive, so A reaches C through B automatically', 'Only if all three VNets are in the same region and the same subscription', 'Only if the peering between A and B has gateway transit enabled'],
 70: ['To replicate every resource automatically to the second region at no extra cost', '*To coordinate platform updates and provide a basis for disaster recovery between two regions', 'To keep both regions inside the same availability zone for lower latency', 'To allow a single virtual network to span two regions without peering'],
 132: ['Declarative templates deploy faster because Azure skips resource validation', 'Declarative templates can target several subscriptions in a single deployment', 'Imperative scripts cannot create dependent resources in the correct order', '*You describe the desired end state and the platform works out the steps, so the deployment is repeatable'],
 85: ['Azure Policy compliance alerts', 'Azure Advisor reliability alerts', 'Azure Monitor metric alerts', '*Azure Service Health'],
 60: ['*Conditional Access in Microsoft Entra ID', 'Azure Policy with an identity initiative', 'Azure role-based access control (RBAC)', 'Microsoft Defender for Cloud security policies'],
 129: ['*It applies automatically — assignments are inherited by every child scope, including ones created later', 'It must be assigned again on the new subscription, because assignments are copied at creation time', 'It applies only if the new subscription is moved into the same resource group as the policy', 'It applies only to resources that existed when the assignment was made, not to new ones'],
 41: ['A private repository for the organization’s own templates and images', 'The tenant-wide catalog of Entra ID applications available for single sign-on', 'The portal section where usage and spending across subscriptions is reviewed', '*A catalog of preconfigured solutions and images from Microsoft and partners, ready to deploy'],
 95: ['It cannot be combined with reserved capacity or hybrid licensing discounts', 'It always costs more than owning hardware once the workload runs steadily', 'It does not allow scaling down until the end of the billing month', '*Monthly spend can vary and is harder to predict exactly without monitoring tools'],
 88: ['*In the Trust Center / Service Trust Portal, where audit reports and certifications are published', 'In Microsoft Defender for Cloud, under the regulatory compliance dashboard for the subscription', 'By opening a support request, since compliance evidence is only released case by case', 'In the Azure portal under Azure Policy, in the compliance section of the built-in initiative'],
 17: ['HA covers planned maintenance windows; DR covers unplanned incidents of any size', 'DR applies to data only; HA applies to compute and networking components', 'HA is achieved with backups; DR is achieved with redundant instances', '*HA keeps the service up through minor failures; DR restores the service after a major outage'],
 105: ['Only if the two VMs are in different subnets of that VNet', 'Only for management traffic such as RDP and SSH between the VMs', '*No — the default rules allow traffic inside the VNet and outbound to the internet; only inbound internet traffic is denied', 'Yes — the default rules deny all traffic, including between VMs in the same VNet'],
 103: ['*It provides RDP/SSH to a VM through the portal without a public IP or open inbound ports on the VM', 'It encrypts the VM disks and the traffic between the VM and the portal end to end', 'It applies operating system patches to the VM without requiring a public IP address', 'It removes the need for VM credentials by authenticating sessions with Entra ID only'],
 15: ['*You pay only for the resources consumed, during the period they are consumed', 'You pay a fixed monthly fee per subscription regardless of the resources used', 'You pay a fixed annual amount agreed in advance, adjusted at the end of the year', 'You pay a perpetual license upfront and then only for support and updates'],
 112: ['Yes — Basic allows technical tickets, but only for security-related incidents', '*No — Basic covers billing and subscription management, documentation and community forums; no technical tickets', 'Yes — Basic allows technical tickets, but only during business hours and by email', 'Yes — Basic includes technical support tickets with a 24-hour initial response time'],
 50: ['Reviewing and removing role assignments automatically after a fixed period', 'Requiring multi-factor authentication before any privileged role is used', 'Assigning built-in roles only at the subscription scope, never on resources', '*Granting each user only the permissions strictly necessary for their task'],
 59: ['The portal where subscriptions and billing accounts for the tenant are managed', 'A support tier that includes security reviews of the customer’s deployment', '*The portal where Microsoft documents its security, privacy, and compliance posture', 'The dashboard where network and security monitoring alerts are reviewed'],
 72: ['*A management group containing those subscriptions', 'A blueprint definition assigned to those subscriptions', 'A shared resource group linked from those subscriptions', 'A policy tag applied to every resource in those subscriptions'],
 51: ['Azure Monitor with security insights', '*Microsoft Defender for Cloud', 'Azure Policy with the security initiative', 'Azure Advisor security recommendations'],
 117: ['The free tier includes every feature; paid plans only add longer data retention', '*The free tier provides posture management and Secure Score; paid plans add active threat detection per workload', 'There is no free tier — every feature of Defender for Cloud requires a paid plan', 'The free tier covers virtual machines only; paid plans are required for other resources'],
 106: ['Resource tags referenced in the rule', 'Availability sets used as the rule source', '*Application Security Groups (ASGs)', 'Azure Firewall network rule collections'],
 66: ['The customer commits to a number of executions per month in exchange for a lower price', 'The provider reserves dedicated hosts and the customer pays a fixed monthly amount', '*The provider manages the infrastructure and the customer pays per execution, not for servers', 'The customer manages the operating system and the provider manages only the hardware'],
 123: ['Inbound (ingress) data transfer into Azure is charged per GB; outbound is free', 'Traffic inside the same region is charged at a flat rate; cross-region traffic is free', '*Outbound (egress) data transfer leaving Azure is charged per GB; inbound is generally free', 'Data transfer is charged only when it crosses between two different subscriptions'],
 74: ['AKS runs Linux containers only; ACI runs both Windows and Linux containers', 'ACI orchestrates full clusters; AKS runs single isolated containers on demand', '*ACI runs single containers quickly with no orchestration; AKS orchestrates full clusters with scaling and self-healing', 'ACI is billed per node hour; AKS is billed per second of container execution'],
 16: ['It prevents failures entirely by replacing components before they wear out', '*It lets the service keep running even if an individual component fails', 'It restores the service from backups after a datacenter-wide outage', 'It guarantees that data is never lost, even when a whole region fails'],
 79: ['They are the same directory service, offered as cloud-hosted and self-hosted editions', 'AD DS is the cloud-native option; Entra ID is the on-premises option for domain-joined devices', 'Entra ID handles device management only; AD DS handles user sign-in and SSO', '*Entra ID is an HTTP/REST-based cloud identity service; AD DS uses LDAP and Kerberos designed for on-premises networks'],
};
