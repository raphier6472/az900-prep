// '*' marks the correct option (stripped on merge)
module.exports = [
 { id: 141, dom: 'd1', obj: '1.1', q: 'A retailer keeps its customer database in its own datacenter for data-residency reasons but hosts its public website in Azure, with the two connected over a VPN. Which statement about this setup is accurate?', opts: ['It is a multi-cloud model, because two separately managed environments host parts of one solution', 'It is a private cloud model, because the data never leaves infrastructure the retailer controls', '*It is a hybrid cloud model, because on-premises and public cloud resources form one connected solution', 'It is a public cloud model, because the customer-facing component runs in a public cloud'], explain: 'Hybrid cloud = on-premises infrastructure and public cloud working together as one solution. Multi-cloud would mean two public cloud providers; private cloud would mean everything on dedicated infrastructure.' },
 { id: 142, dom: 'd1', obj: '1.1', q: 'Under the shared responsibility model, which item is the customer’s responsibility in EVERY service model, including SaaS?', opts: ['*Classifying and protecting the data, and the accounts and identities that access it', 'Patching the operating system and middleware that run the application', 'Securing the physical hosts, networks and datacenters that run the service', 'Configuring the runtime and the application platform used by the service'], explain: 'Data, endpoints, accounts and access management always stay with the customer, even in SaaS. OS/middleware move to the provider in PaaS and SaaS; physical infrastructure is always the provider’s.' },
 { id: 143, dom: 'd1', obj: '1.2', q: 'A company runs its web tier across two availability zones behind a load balancer, and also replicates its database to a second region with a documented manual failover. Which pairing describes what each measure delivers?', opts: ['Zones deliver disaster recovery; the second region delivers scalability', 'Zones deliver elasticity; the second region delivers high availability', 'Zones deliver predictability; the second region delivers fault tolerance', '*Zones deliver high availability; the second region delivers disaster recovery'], explain: 'Spreading instances across zones keeps the service running through a datacenter failure (high availability). A copy in another region that you fail over to after a major outage is disaster recovery.' },
 { id: 144, dom: 'd1', obj: '1.3', q: 'One team deploys a web app to Azure App Service; a second team runs the same app on Azure VMs they built themselves. Which responsibility differs between the two teams?', opts: ['Only the App Service team is responsible for the application code and its configuration', '*Only the VM team is responsible for patching and hardening the operating system', 'Only the VM team is responsible for the security of the data stored by the app', 'Only the App Service team is responsible for the identities that sign in to the app'], explain: 'App Service is PaaS: Microsoft manages the OS and runtime. VMs are IaaS: the customer patches and hardens the OS. Code, data and identities remain the customer’s responsibility in both models.' },
 { id: 145, dom: 'd1', obj: '1.1', q: 'Which statement correctly distinguishes capital expenditure (CapEx) from operational expenditure (OpEx) when moving to the cloud?', opts: ['CapEx is the ongoing pay-as-you-go spend; OpEx is the upfront purchase of datacenter hardware', 'CapEx and OpEx both disappear in the cloud, since the provider owns all of the costs', '*Moving to the cloud shifts spending from upfront purchases (CapEx) to ongoing consumption (OpEx)', 'Moving to the cloud increases CapEx, because reserved capacity must be purchased upfront'], explain: 'CapEx = buying assets upfront (servers, datacenter). OpEx = paying for what you use as you go. The cloud replaces most CapEx with OpEx; reservations are still billed as consumption, not as owned assets.' },
 { id: 146, dom: 'd2', obj: '2.1', q: 'One VM must survive the failure of an entire datacenter in its region, while a second VM only needs protection against a single rack or host failure. Which combination is appropriate?', opts: ['*Availability zones for the first VM; an availability set for the second VM', 'Region pairs for the first VM; availability zones for the second VM', 'An availability set for the first VM; a scale set for the second VM', 'Availability zones for the first VM; a region pair for the second VM'], explain: 'Availability zones are separate datacenters within a region. Availability sets spread VMs across fault domains (racks) and update domains inside one datacenter. Region pairs are about cross-region DR, not a single VM.' },
 { id: 147, dom: 'd2', obj: '2.2', q: 'A company must connect its datacenter to Azure with predictable throughput and without traffic crossing the public internet; a small branch office needs an encrypted connection over its existing internet link. Which pairing fits?', opts: ['VNet peering for the datacenter; ExpressRoute for the branch office', 'Point-to-Site VPN for the datacenter; ExpressRoute for the branch office', 'Azure Front Door for the datacenter; Site-to-Site VPN for the branch office', '*ExpressRoute for the datacenter; Site-to-Site VPN for the branch office'], explain: 'ExpressRoute is a private, dedicated circuit that bypasses the internet. A Site-to-Site VPN encrypts traffic between an on-premises gateway and Azure over the public internet — right for a branch.' },
 { id: 148, dom: 'd2', obj: '2.2', q: 'A global web app must route users to the nearest healthy region and also inspect HTTP requests with a web application firewall at the edge. Which service covers both needs in one product?', opts: ['Azure Traffic Manager with a performance routing profile', '*Azure Front Door with a WAF policy enabled', 'Azure Load Balancer in each region with health probes', 'Azure Application Gateway deployed in the primary region'], explain: 'Front Door is a global layer-7 entry point: it routes to the closest healthy backend and supports WAF. Traffic Manager is DNS-only (no WAF); Load Balancer is layer 4 and regional; Application Gateway is regional.' },
 { id: 149, dom: 'd2', obj: '2.3', q: 'A storage account must keep data readable if the entire primary region is lost, and must also stay fully available if a single datacenter in the primary region fails. Which redundancy option satisfies both?', opts: ['*Read-access geo-zone-redundant storage (RA-GZRS)', 'Read-access geo-redundant storage (RA-GRS)', 'Zone-redundant storage (ZRS)', 'Geo-redundant storage (GRS)'], explain: 'GZRS copies data across zones in the primary region (surviving a datacenter loss) and to a secondary region; the RA- variant makes the secondary readable without a failover. RA-GRS is only LRS in the primary; ZRS has no second region.' },
 { id: 150, dom: 'd2', obj: '2.3', q: 'Which access tier is the right choice for monthly invoice PDFs that must be retained for years and are rarely opened, but when requested must be available within milliseconds?', opts: ['Archive tier, because rehydration happens automatically on first access', 'Hot tier, because retention period does not influence the tier choice', '*Cool or cold tier, which stays online while charging less for storage', 'Premium tier, because it is optimized for infrequently accessed data'], explain: 'Cool and cold are online tiers with lower storage cost for infrequent access. Archive is offline: reading requires rehydration that takes hours. Hot costs more to store; premium is for high-performance workloads.' },
 { id: 151, dom: 'd2', obj: '2.4', q: 'An organization wants partners from other companies to access a shared app with their own corporate credentials, without creating internal accounts for them. Which Microsoft Entra feature applies?', opts: ['Microsoft Entra Domain Services with a trust to each partner domain', 'Conditional Access with a named location for each partner company', 'Microsoft Entra Privileged Identity Management with guest role activation', '*Microsoft Entra External ID B2B collaboration (guest users)'], explain: 'B2B collaboration invites external users as guests who sign in with their own identity provider. Domain Services, Conditional Access and PIM do not create cross-organization access on their own.' },
 { id: 152, dom: 'd2', obj: '2.4', q: 'A user has the Reader role at the subscription scope and the Contributor role on one resource group in that subscription. What can the user do?', opts: ['Read everything in the subscription and modify resources anywhere in it', '*Read everything in the subscription and modify resources only in that resource group', 'Modify resources only in that resource group, with no visibility of the rest of the subscription', 'Read everything in the subscription; the Contributor assignment is ignored because Reader is broader'], explain: 'RBAC is additive: permissions are the union of all assignments. Reader at the subscription gives read access everywhere; Contributor at the resource group adds write access there only.' },
 { id: 153, dom: 'd2', obj: '2.4', q: 'Which statement about the Zero Trust security model is accurate?', opts: ['*Every request is verified explicitly, regardless of whether it originates inside or outside the corporate network', 'Traffic that originates inside the corporate network is trusted once the perimeter firewall has admitted it', 'Users receive standing administrator access so that verification is not needed on every request', 'It is a product that replaces MFA, Conditional Access and RBAC with a single sign-in policy'], explain: 'Zero Trust principles: verify explicitly, use least-privilege (just-in-time) access, and assume breach. It is a model applied with tools like MFA, Conditional Access and RBAC — not a product that replaces them.' },
 { id: 154, dom: 'd3', obj: '3.1', q: 'Two identical VMs run in different regions. Their monthly costs differ even though size, OS and running hours are the same. What explains the difference?', opts: ['Costs differ because one VM has a resource lock, which is billed separately', 'Costs differ because one subscription has more resource groups than the other', '*Costs differ because Azure prices the same VM size differently by region', 'Costs differ because one VM is in a management group with a policy assigned'], explain: 'Region is a cost factor: the same SKU can have different prices in different regions. Locks, resource groups, management groups and policies are free and do not change VM pricing.' },
 { id: 155, dom: 'd3', obj: '3.1', q: 'A finance team wants to see which department generated which costs in a subscription where all resources share the same resource groups. What is the most practical mechanism?', opts: ['Create a separate subscription per department and move every resource into it', 'Apply a resource lock per department so that costs are tracked per lock', 'Assign a management group per department and read the costs at that scope', '*Apply a department tag to each resource and group cost analysis by that tag'], explain: 'Tags are name/value metadata on resources; Cost Management can filter and group costs by tag, which is the standard way to attribute spend across teams without restructuring subscriptions.' },
 { id: 156, dom: 'd3', obj: '3.2', q: 'A policy with the deny effect is assigned to a subscription after several non-compliant resources already exist. What happens to those existing resources?', opts: ['They are deleted automatically at the next compliance evaluation', '*They remain, are reported as non-compliant, and cannot be updated in a way that stays non-compliant', 'They are automatically modified so that they become compliant with the policy', 'They are exempt permanently, because deny only applies to resources created after the assignment'], explain: 'Deny blocks non-compliant create/update requests going forward. Existing resources are not deleted or changed — they are flagged as non-compliant in the compliance view until remediated.' },
 { id: 157, dom: 'd3', obj: '3.2', q: 'A resource group has a ReadOnly lock. An administrator with the Owner role tries to add a tag to a VM in that resource group. What is the outcome?', opts: ['*The operation is denied, because a ReadOnly lock blocks modifications for all users, including Owners', 'The operation succeeds, because Owner permissions take precedence over resource locks', 'The operation succeeds, because tags are metadata and are not covered by locks', 'The operation is denied only if the lock was created at the subscription scope'], explain: 'Locks apply regardless of role: a ReadOnly lock allows only read operations on the locked scope and everything under it. To modify the resource, someone with permission must remove the lock first.' },
 { id: 158, dom: 'd3', obj: '3.3', q: 'A team wants to deploy the same environment repeatedly from a template and needs the deployment to be idempotent, so re-running it never creates duplicates. Which statement is TRUE?', opts: ['Only Bicep is idempotent; ARM JSON templates create duplicates when re-run', 'Only Azure CLI scripts are idempotent, because each command checks for existence first', 'Idempotency requires Terraform; native Azure tooling cannot guarantee it', '*Both ARM templates and Bicep are declarative and idempotent; a CLI script has to handle that itself'], explain: 'ARM templates and Bicep describe the desired state, and Resource Manager reconciles existing resources to it — re-running is safe. Imperative CLI scripts must add their own checks to avoid duplicates or errors.' },
 { id: 159, dom: 'd3', obj: '3.4', q: 'An application reports slow responses, but the VM’s CPU and memory metrics look normal. Which tool would most directly show which request or dependency is slow?', opts: ['Azure Service Health, which lists degraded dependencies per region', '*Application Insights, which traces requests and dependency calls inside the app', 'Azure Advisor, which recommends performance fixes for the VM size', 'Resource Health, which reports the availability state of the VM'], explain: 'Application Insights instruments the application itself: request durations, dependency calls (SQL, HTTP), exceptions. VM-level metrics and platform health tools cannot see inside the app.' },
 { id: 160, dom: 'd3', obj: '3.4', q: 'A team receives an alert that a region-wide storage incident is in progress. Which tool sent it, and how does it differ from a metric alert?', opts: ['Azure Advisor — it warns about incidents, while metric alerts warn about cost anomalies', 'Resource Health — it reports platform incidents, while metric alerts cover only logs', '*Service Health — it reports Azure platform incidents, while metric alerts react to values from your own resources', 'Azure Monitor Logs — it stores incident data, while metric alerts are configured in Advisor'], explain: 'Service Health alerts come from Microsoft about the platform (incidents, maintenance, advisories). Metric alerts are rules you define on telemetry from your own resources, such as CPU or request count.' },
];
